.DS_Store
Thumbs.db
*.log
node_modules/
.vscode/
.idea/

# Defense-in-depth: keep common secret/credential file patterns out of
# the repo even though we don't currently use them. Found-by:
# 'ecc security-review' static-site checklist.
.env
.env.*
*.env
secrets.json
secrets.*.json
credentials.json
credentials.*.json
*.pem
*.key
*.p12
*.pfx
client_secret*.json
service_account*.json
gcloud-key*.json
.npmrc

# KB3D Enchanted GLBs are hosted on pCloud Public Folder, fronted by the
# Cloudflare Worker at niwa-cors.yuichi916.workers.dev which re-serves them
# with CORS=*. See _workers/pcloud-cors-proxy.js. Keeps repo small.
assets/blender/enc_*.glb
# v671 EXCEPTION: the re-extracted texture-fixed interior props are served
# from the repo (lazyLoadAsset _LOCAL_FIXED) because the pCloud copies
# still carry the broken 1x1-placeholder textures.  1.3-4.8MB each.
!assets/blender/enc_int_*.glb
!assets/blender/enc_tree_*.glb

# Local git worktrees for isolated feature branches
.worktrees/

_test_assets/
_blender/__pycache__/
.superpowers/
